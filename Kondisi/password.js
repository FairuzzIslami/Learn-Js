let password = prompt("masukkan password yang wifi sklh: ")

if (password == "indonesia-1945"){ 
    console.log("selamat password anda benar!!!") // tetapi 
}else{
    return "yeee salaah blok"
}
 //di karenakan syntax == ini operator perbandingan menyamakan nilai yang kita ingin kan
 //tetapi lebih bagus nya menggunakan === karena dia membandingkan yang variabel nya tipe nya sama
